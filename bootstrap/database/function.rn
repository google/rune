//  Copyright 2021 Google LLC.
//
// Licensed under the Apache License, Version 2.0 (the "License")
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

use sym

use root
use bigint
use location
use filepath
use value
use expr
use ident
use statement
use template
use block
use variable

import cbackend as c
import types

enum FuncType {
  Plain  // Includes methods.
  Operator  // Overloaded operator.
  Constructor
  Destructor
  Package  // Initializes all modules in the package.
  Module  // Initializes the module.
  Iterator
  Final
  Struct
  Enum
  Transformer
  Unittest
}

// Function linking type.
enum Linkage {
  Module  // Default, like Python, files in the same directory can access.
  Package  // Allow files in other directories to access.
  Libcall  // A library call API.  All params must have default values.
  Rpc  // An RPC API.  All params must have default values.
  Builtin  // Globally visible, never a lib call or RPC.
  ExternC  // An external C function.
  ExternRpc  // An external RPC function.
}

// Return a name for the function type.
func getFuncTypeName(type: FuncType) -> string {
  switch type {
    FuncType.Plain => return "func"  // Includes methods.
    FuncType.Unittest => return "unittest"  // Includes methods.
    FuncType.Constructor => return "class"
    FuncType.Destructor => return "destructor"
    FuncType.Package => return "package"  // Initializes all modules in the package.
    FuncType.Module => return "module"  // Initializes the module.
    FuncType.Iterator => return "iterator"
    FuncType.Operator => return "operator"
    FuncType.Final => return "final"
    FuncType.Struct => return "struct"
    FuncType.Enum => return "enum"
    FuncType.Transformer => return "transformer"
  }
}

// Create a new function.
class Function(self, parent: Function?, type: FuncType, sym: Sym,
    linkage: Linkage, location: Location) {
  self.type = type
  self.sym = sym
  self.linkage = linkage
  self.location = location
  self.isExtern = linkage == Linkage.ExternC || linkage == Linkage.ExternRpc
  self.opType = ExprType.Add  // Used for overloaded operators.
  // The function's type, set by the typechecker.  Syms are interned by
  // name, so sym.typedValue is unreliable when functions share a name
  // (e.g. the same operator overloaded by two classes).
  self.typedValue = null(types.Type)
  // The body needs structural knowledge of unconstrained parameters and
  // typechecks again under a concrete instantiation before emission.
  self.deferredTypecheck = false
  self.deferredRetypechecked = false
  // Set during C emission when this function directly uses an explicit SIMD
  // builtin. Such functions opt into the corresponding target attribute;
  // ordinary generated functions retain the baseline target.
  self.usesF64x4 = false
  self.usesU8x16 = false
  // The specialization key whose bindings this body's shared expression
  // types were last re-typechecked under (per-signature materialization).
  self.lastMaterializedSpec = ""
  // Specialization keys whose materialization round ERRORED (an
  // unreachable typeswitch arm's recorded instantiation, e.g. the turing
  // test's decParens(())): emission must skip them.
  self.failedSpecs = arrayof(string)
  // True while this body is being materialized: a recursive call must not
  // re-enter (its poly is already instantiated; the spec cache handles
  // same-signature recursion, this flag blocks differing-signature
  // descent into the same frame).
  self.materializeActive = false
  // Published only after the complete transitive withRegion graph validates.
  // The C backend may clone this monomorphic body for compact-region use;
  // ordinary emission always remains available under its original name.
  self.regionCompactEligible = false
  if !isnull(parent) {
    parent.appendChildFunction(self)
    Ident(parent!, self, sym, location)
  }
  subBlock = Block(location)
  // Assume it can return until we learn otherwise.  This is only an issue when
  // evaluating recursive functions.
  subBlock.canReturn = true
  self.insertSubBlock(subBlock)

  // Return the name of the function.
  func name(self) -> string {
    return self.sym.name
  }

  func dumpParams(self, var text: string) {
    firstTime = true
    for variable in self.variables() {
      if !firstTime {
        text += ", "
      }
      firstTime = false
      if !variable.isParam {
        return
      }
      variable.dumpToString(text)
    }
  }

  // Dump the function to the end of |string| for debugging purposes.
  func dumpToString(self, var text: string) {
    if self.type == FuncType.Enum {
      self.dumpEnumToString(text)
      return
    }
    root.indent(text)
    dumpFuncHeader(self, text)
    self.dumpParams(text)
    text += ")"
    if !isnull(self.typeExpr) {
      text += " -> "
      self.typeExpr.dumpToString(text)
    }
    if self.linkage == Linkage.ExternC || self.linkage == Linkage.ExternRpc {
      text += "\n"
    } else {
      text += " {\n"
      getRoot().incrementIndent()
      self.subBlock.dumpToString(text)
      for function in self.childFunctions() {
        function.dumpToString(text)
      }
      getRoot().decrementIndent()
      getRoot().indent(text)
      text += "}\n"
    }

    func dumpFuncHeader(self: Function, var text: string) {
      if self.linkage == Linkage.ExternC {
        text.concat("extern \"C\" func %s" % self.name())
      } else if self.linkage == Linkage.ExternRpc {
        text.concat("rpc %s" % self.name())
      } else if self.type == FuncType.Final {
        text.concat("final")
      } else {
        text.concat("%s %s" % (getFuncTypeName(self.type), self.name()))
      }
      if self.type == FuncType.Constructor {
        template = self.template
        if template.refWidth != root.defaultRefWidth {
          text.concat(":u%u" % template.refWidth)
        }
      }
      text.append('(')
    }
  }

  // Dump an enumerated type to the string.
  func dumpEnumToString(self: Function, var text: string) {
    root.indent(text)
    text.concat("enum %s {\n" % self.name())
    getRoot().incrementIndent()
    for variable in self.variables() {
      root.indent(text)
      variable.dumpToString(text)
      text.append('\n')
    }
    getRoot().decrementIndent()
    root.indent(text)
    text.concat("}\n")
  }

  func genC(self: Function, builder: c.CBuilder) {
    switch self.type {
      FuncType.Module => self.genModule(builder)
      FuncType.Plain => self.genCPlainFunc(builder)
      FuncType.Operator => self.genCPlainFunc(builder)
      FuncType.Struct => self.genCStruct(builder)
      FuncType.Constructor => self.genCConstructor(builder)
      // Enum members are inlined as integer constants at their use sites.
      FuncType.Enum => return
      // Iterators are inlined into foreach statements by the typechecker;
      // nothing is emitted for the iterator function itself.
      FuncType.Iterator => return
      // Transformers are compile-time code generators.
      FuncType.Transformer => return
      default => raise Status.Unimplemented, "Function type not yet supported by C backend ", self.type
    }
  }

  func genModule(self: Function, builder: c.CBuilder) {
    // Imported modules are appended as child functions after the importing
    // module's own children; emit them first so their top-level statements
    // run before the importer's (matching typechecking order).
    for childfunc in self.childFunctions() {
      if childfunc.type == FuncType.Module {
        childfunc.genC(builder)
      }
    }
    for childfunc in self.childFunctions() {
      if childfunc.type != FuncType.Module {
        childfunc.genC(builder)
      }
    }
    for statement in self.subBlock.statements() {
      // Note that a Rune statement may correspond to a C statement in main()
      // or a C top-level declaration.
      statement.genC(builder)
    }
  }


  func genCPlainFunc(self: Function, builder: c.CBuilder) {
    if self.isExtern {
      // extern "C" functions are provided by the C runtime (under an rn_
      // prefix); nothing is emitted for the declaration itself.
      return
    }
    // TODO: figure out storage types.....

    // Is this a polymorphic type?  polymorphic type variables do not
    // correspond to anything in C (though we could try void*). Instead
    // we must iterate over all known instantiations -- this only works
    // because we are doing whole-program analysis before we generate
    // the C.  (i.e., it is not clear how this would work if we try to
    // compile different packages into separate C files).

    fntype = self.typedValue
    if isnull(fntype) {
      fntype = self.sym.typedValue
    }
    assert !isnull(fntype)

    if fntype.type == types.Type.TypeClass.Polymorphic {
      /******** Polymorphic function: iterate over instantiations *********/
       pi = fntype.poly.instantiations
       if !isnull(pi) {
         for instantiation in pi {
           builder.resolveInstantiation(instantiation)
           // Instantiations whose parameter bindings still contain type
           // variables only resolve under an outer instantiation (e.g. a
           // call from a polymorphic class constructor); they are emitted
           // on demand from the call site while that outer binding is
           // active.  A free RESULT slot (a generalized result variable
           // recorded at a call site) is emittable: the body's return
           // resolves it.
           if builder.instantiationParamsConcrete(instantiation) {
             self.genCPolyInstantiation(builder, instantiation)
           }
         }
      }
    } else {
      /******************* Monomorphic function *************************/
      emittedSym = self.sym
      if builder.compactRegionEmission {
        if !self.regionCompactEligible {
          builder.typechecker.typeErrorOnce(
              "internal: compact region emission reached an unapproved function",
              self.location)
          return
        }
        compactName = builder.compactFunctionCName(self)
        if !isnull(builder.findDeclaration(compactName)) {
          return
        }
        emittedSym = Sym.new(compactName)
        emittedSym.typedValue = fntype
      }
      // open new scope - new parameter bindings are added to the scope
      // automatically
      builder.openScope()
      builder.openParamList()
      for variable in self.variables() {
        param = variable.genCParameter(builder)  // param is added to new scope
        builder.addParam(param)
      }
      paramList = builder.closeParamList()
      builder.openDependencyList();
      savedTryBase = builder.openFunctionTryContext()
      // Mark that a function body is being emitted so its first-assigned
      // locals (including loop variables) shadow any same-named module
      // variable instead of sharing the module static.
      monoPrevEmittingFn = builder.currentEmittingFn
      monoPrevEmittingName = builder.currentEmittingName
      self.usesF64x4 = false
      self.usesU8x16 = false
      builder.currentEmittingFn = self
      builder.currentEmittingName = emittedSym.name
      builder.openBlock(self.subBlock.location)
      for bodyStatement in self.subBlock.statements() {
        bodyStatement.genC(builder)
      }
      self.emitScopeExitFinals(builder)
      body = builder.closeBlock()
      monoUsesF64x4 = self.usesF64x4
      monoUsesU8x16 = self.usesU8x16
      builder.currentEmittingFn = monoPrevEmittingFn
      builder.currentEmittingName = monoPrevEmittingName
      builder.closeFunctionTryContext(savedTryBase)
      builder.closeScope()
      fundec = builder.cFunDef(self.location, emittedSym, paramList, body,
          monoUsesF64x4, monoUsesU8x16)
      // The return type appears in the function prototype before the body can
      // call the tuple constructor. Ensure and depend on its typedef directly;
      // relying on the body dependency is too late for declaration ordering.
      monoResultType = builder.typechecker.resolve(fntype.function.result!)
      if monoResultType.type == types.Type.TypeClass.Tuple {
        builder.ensureTupleDeclared(self.location, monoResultType)
        fundec.addDependencyName(
            builder.typeGenerator.genCTuple(monoResultType.tuple!).name)
      } else if monoResultType.type == types.Type.TypeClass.Array {
        monoResultArray = builder.ensureArrayDeclared(
            self.location, monoResultType.array.elementType)
        fundec.addDependencyName(monoResultArray.typeName)
      }
      // Every named parameter type must precede the function prototype. Array
      // parameters used to depend on the body happening to instantiate the
      // same array first; a helper that only iterates an incoming [u8] could
      // therefore emit `u8_array_t` before its typedef.
      for monoVar in self.variables() {
        if monoVar.isParam && !isnull(monoVar.typedValue) {
          monoPt = builder.typechecker.resolve(monoVar.typedValue!)
          if monoPt.type == types.Type.TypeClass.Array {
            monoArray = builder.ensureArrayDeclared(
                self.location, monoPt.array.elementType)
            fundec.addDependencyName(monoArray.typeName)
          } else if monoPt.type == types.Type.TypeClass.Tuple {
            builder.ensureTupleDeclared(self.location, monoPt)
            fundec.addDependencyName(
                builder.typeGenerator.genCTuple(monoPt.tuple!).name)
          } else if builder.isClassType(monoPt) {
            fundec.addDependencyName(builder.classCName(monoPt) + "_class")
          } else if monoPt.type == types.Type.TypeClass.Function {
            builder.ensureFunctionPtrDeclared(self.location, monoPt)
            monoFnptrCType = builder.typeGenerator.genCFunction(monoPt.function!)
            if !isnull(monoFnptrCType) {
              fundec.addDependencyName(monoFnptrCType.cDefinedType.name)
            }
          }
        }
      }
      fundec.addDependencies(builder.topDependencyList().names)
      builder.closeDependencyList()
      builder.addDeclaration(fundec)
    }
  }

  // Rune frees an object when its last reference drops.  The C backend has no
  // reference counts, so it approximates the common case: a function that
  // returns nothing cannot hand a locally built array of objects back to its
  // caller, so when it returns those objects are unreachable and their
  // destructors must run.  Only object arrays that are plain locals (not
  // parameters) of a void function qualify; anything that might escape (a
  // non-void return, an object stored in a relation or global) is left alone.
  // Must be called while the function body block and parameter scope are still
  // open so the array locals resolve and the loop index declares into the body.
  func emitScopeExitFinals(self: Function, builder: c.CBuilder) {
    fntype = self.typedValue
    if isnull(fntype) {
      return
    }
    if fntype.type != types.Type.TypeClass.Function {
      return
    }
    resTy = builder.typechecker.resolve(fntype.function.result!)
    isVoid = (resTy.type == types.Type.TypeClass.Typename &&
              resTy.tyname.name.name == "none")
    if !isVoid {
      return
    }
    // Function locals are not tracked as Variables, so find the object arrays
    // from their defining assignments: a top-level `name = <array-of-class>`
    // statement.  Each local is destroyed once, in declaration order.
    seen = arrayof(string)
    for statement in self.subBlock.statements() {
      if (statement.type == StateType.Assign && !isnull(statement.expr) &&
          statement.expr.type == ExprType.Equals) {
        lhs = statement.expr.firstChildExpr
        if (!isnull(lhs) && lhs.type == ExprType.Ident &&
            !isnull(lhs.typedValue) && !isnull(lhs.val.symVal)) {
          vty = builder.typechecker.resolve(lhs.typedValue!)
          if vty.type == types.Type.TypeClass.Array {
            sym = lhs.val.symVal!
            already = false
            for s in seen {
              if s == sym.name {
                already = true
              }
            }
            if !already {
              elemTy = vty.array.elementType!
              destroyName = builder.classDestroyName(elemTy)
              if destroyName == "" {
                destroyName = builder.classFinalName(elemTy)
              }
              if destroyName != "" {
                seen.append(sym.name)
                self.emitArrayElementFinals(builder, sym, vty, destroyName)
              }
            }
          }
        }
      }
    }
  }

  // Emit `for (i = 0; i < array_length(arr); i++) if (arr[i]) destroy(arr[i]);`
  // into the current (open) block, destroying every live element of |arrSym|.
  func emitArrayElementFinals(self: Function, builder: c.CBuilder, arrSym: Sym,
                              arrTy: types.Type, destroyName: string) {
    loc = self.location
    u64type = builder.cIntType(loc, false, 64u32)
    idxSym = Sym.new("%s_finalidx" % arrSym.name)
    idxDecl = builder.declareLocal(idxSym, u64type)
    zero = Bigint(0u64)
    init = c.CExpr(c.CBinaryOperator(loc, c.CBinaryOperator.Type.Assign,
        null(c.CTypeExpr), idxDecl, builder.cIntLiteral(zero)))
    lenCall = builder.cCall(loc, c.CRuntimeFunction.ArrayLength,
        builder.cIdentifier(arrSym, false, arrTy))
    idxRef1 = c.CExpr(c.CIdentifier(idxSym, null(c.CTypeExpr), false, false, false))
    cond = c.CExpr(c.CBinaryOperator(loc, c.CBinaryOperator.Type.LessThan,
        null(c.CTypeExpr), idxRef1, lenCall))
    one = Bigint(1u64)
    idxRef2 = c.CExpr(c.CIdentifier(idxSym, null(c.CTypeExpr), false, false, false))
    idxRef3 = c.CExpr(c.CIdentifier(idxSym, null(c.CTypeExpr), false, false, false))
    plus = c.CExpr(c.CBinaryOperator(loc, c.CBinaryOperator.Type.Plus,
        null(c.CTypeExpr), idxRef2, builder.cIntLiteral(one)))
    incr = c.CExpr(c.CBinaryOperator(loc, c.CBinaryOperator.Type.Assign,
        null(c.CTypeExpr), idxRef3, plus))
    forList = c.CExprList()
    forList.appendCExpr(init)
    forList.appendCExpr(cond)
    forList.appendCExpr(incr)
    builder.openBlock(loc)
    idxRef4 = c.CExpr(c.CIdentifier(idxSym, null(c.CTypeExpr), false, false, false))
    condElem = c.CExpr(c.CIndexExpr(loc,
        builder.cIdentifier(arrSym, false, arrTy), idxRef4))
    idxRef5 = c.CExpr(c.CIdentifier(idxSym, null(c.CTypeExpr), false, false, false))
    callElem = c.CExpr(c.CIndexExpr(loc,
        builder.cIdentifier(arrSym, false, arrTy), idxRef5))
    destroyArgs = c.CExprList()
    destroyArgs.appendCExpr(callElem)
    builder.openBlock(loc)
    builder.addStatement(loc, builder.cCall(loc, destroyName, c.CExpr(destroyArgs)))
    ifBody = builder.closeBlock()
    builder.addStatement(loc,
        c.CStatement(loc, c.CStatementType.If, condElem, ifBody))
    loopBody = builder.closeBlock()
    builder.addStatement(loc,
        c.CStatement(loc, c.CStatementType.For, c.CExpr(forList), loopBody))
  }

  // Emit a single specialization of a polymorphic function under the given
  // instantiation.  Also called on demand from poly call sites, while an
  // outer instantiation binding is active.
  // The ground result type of an emitted call inside a spec body:
  // compose the callee's spec name from recovered argument types and
  // look it up among already-emitted specs (nested emissions complete
  // bottom-up, so a recursive callee's def precedes the parent's
  // signature assembly).
  func emittedCallResult(self: Function, builder: c.CBuilder,
                         callExpr: Expr) -> types.Type? {
    if callExpr.type != ExprType.Call {
      return null(types.Type)
    }
    ecrLeft = callExpr.firstChildExpr
    if isnull(ecrLeft) {
      return null(types.Type)
    }
    if ecrLeft.type != ExprType.Ident {
      return null(types.Type)
    }
    ecrArgs = ecrLeft.nextExprChildExpr
    if isnull(ecrArgs) {
      return null(types.Type)
    }
    ecrTypes = arrayof(types.Type)
    if ecrArgs.type == ExprType.List || ecrArgs.type == ExprType.Tuple {
      for ecrA in ecrArgs.childExprs() {
        ecrTypes.append(ecrA.resolvedArgType(builder, 0u32, null(Block)))
      }
    } else {
      ecrTypes.append(ecrArgs.resolvedArgType(builder, 0u32, null(Block)))
    }
    for ecrT in ecrTypes {
      if builder.typechecker.hasFreeVars(ecrT) {
        return null(types.Type)
      }
    }
    ecrName = types.Type(types.TupleType(ecrTypes)).genSpecializationName(
        ecrLeft.val.symVal.name)
    ecrHit = builder.lookupSpecResult(ecrName)
    if !isnull(ecrHit) {
      return ecrHit
    }
    // Not emitted yet (a recursion still unwinding): derive the callee
    // spec's result by walking ITS body under ITS ground params — a
    // read-only, syntax-directed fixpoint.  Record it so recursion
    // converges.
    ecrCallee = builder.findFunction(ecrLeft.val.symVal.name)
    if isnull(ecrCallee) {
      return null(types.Type)
    }
    if isnull(ecrCallee.subBlock) || builder.deriveDepth >= 24u32 {
      return null(types.Type)
    }
    ecrPrevTuple = builder.currentEmittingParamTuple
    ecrPrevPFn = builder.currentEmittingParamFn
    ecrPrevFn = builder.currentEmittingFn
    builder.currentEmittingParamTuple = types.Type(types.TupleType(ecrTypes))
    builder.currentEmittingParamFn = ecrCallee
    builder.currentEmittingFn = ecrCallee
    builder.deriveDepth += 1u32
    ecrDerived = ecrCallee.deriveSpecResult(builder, ecrCallee.subBlock!)
    builder.deriveDepth -= 1u32
    builder.currentEmittingParamTuple = ecrPrevTuple
    builder.currentEmittingParamFn = ecrPrevPFn
    builder.currentEmittingFn = ecrPrevFn
    if !isnull(ecrDerived) {
      if !builder.typechecker.hasFreeVars(ecrDerived!) {
        builder.recordSpecResult(ecrName, ecrDerived!)
        return ecrDerived
      }
    }
    return null(types.Type)
  }

  // Derive a spec's result type from its emitted return path: follow the
  // typeswitch arm the GROUND scrutinee selects, and read return
  // expressions through the live recovery (emitted-spec results for
  // recursive calls first — a recorded instantiation's result can carry
  // another round's binding).  Null when nothing grounds.  READ-ONLY
  // w.r.t. recorded instantiations.
  func deriveSpecResult(self: Function, builder: c.CBuilder,
                        block: Block) -> types.Type? {
    for dsrStmt in block.statements() {
      if dsrStmt.type == StateType.Return {
        if !isnull(dsrStmt.expr) {
          dsrMap = self.emittedCallResult(builder, dsrStmt.expr!)
          if !isnull(dsrMap) {
            return dsrMap
          }
          // Only shapes the live recovery derives SOUNDLY from ground
          // params: an operator/cast expression's slot can be ground yet
          // belong to another spec (genericFactorial's n * fact(n-1)).
          dsrSafe = (dsrStmt.expr.type == ExprType.Ident ||
              dsrStmt.expr.type == ExprType.Constant ||
              dsrStmt.expr.type == ExprType.Index ||
              dsrStmt.expr.type == ExprType.Tuple)
          if dsrSafe {
            dsrCand = dsrStmt.expr.resolvedArgType(builder, 0u32, null(Block))
            if !builder.typechecker.hasFreeVars(dsrCand) {
              return dsrCand
            }
          }
        }
      } else if dsrStmt.type == StateType.Typeswitch {
        if !isnull(dsrStmt.expr) && !isnull(dsrStmt.subBlock) {
          dsrScr = dsrStmt.expr.resolvedArgType(builder, 0u32, null(Block))
          if !builder.typechecker.hasFreeVars(dsrScr) {
            dsrIdx = builder.typechecker.selectTypeswitchCaseFor(dsrStmt, dsrScr)
            dsrI = 0u32
            for dsrCase in dsrStmt.subBlock.statements() {
              if dsrI == dsrIdx {
                if !isnull(dsrCase.subBlock) {
                  dsrArm = self.deriveSpecResult(builder, dsrCase.subBlock!)
                  if !isnull(dsrArm) {
                    return dsrArm
                  }
                }
              }
              dsrI += 1u32
            }
          }
        }
      } else {
        if !isnull(dsrStmt.subBlock) {
          dsrSub = self.deriveSpecResult(builder, dsrStmt.subBlock!)
          if !isnull(dsrSub) {
            return dsrSub
          }
        }
      }
    }
    return null(types.Type)
  }

  func genCPolyInstantiation(self: Function, builder: c.CBuilder,
                             instantiation: types.TyvarInstantiation) {
    fntype = self.typedValue!
    // Consume the call-site handoff immediately so nested emissions
    // never inherit a stale want.
    giWantName = builder.wantSpecName
    giWantParams = builder.wantSpecParams
    giWantResult = builder.wantSpecResult
    builder.wantSpecName = ""
    builder.wantSpecParams = null(types.Type)
    builder.wantSpecResult = null(types.Type)
    // Resolve each parameter's own type under the instantiation.
    // The generalized variables in varBindings do not align
    // positionally with the parameters (operator type variables can
    // be generalized too), so positional lookup is wrong.
    builder.instantiate(fntype.poly!, instantiation)
    paramTypes = arrayof(types.Type)
    for variable in self.variables() {
      if isnull(variable.typedValue) {
        // A parameter that never typechecked (poisoned method).
        paramTypes.append(types.NONETY())
      } else {
        ptResolved = builder.typechecker.resolve(variable.typedValue!)
        if ptResolved.type == types.Type.TypeClass.Tuple {
          // Tuple-typed parameters need the typedef even when no tuple
          // literal has emitted yet.
          builder.ensureTupleDeclared(self.location, ptResolved)
        }
        paramTypes.append(ptResolved)
      }
    }
    // The specialization name comes from the resolved parameter
    // types, matching genSpecializationName at call sites.
    paramTuple = types.Type(types.TupleType(paramTypes))
    specializedName = paramTuple.genSpecializationName(self.sym.name)
    // The call-site handoff is authoritative: the variables-derived
    // name above can resolve through an outer ACTIVE round of this
    // same function (stale slots) and compose the wrong spec, or
    // carry FREE pieces (an operator's unresolved width param).  The
    // call site composed giWantName from GROUND arg types — let the
    // adoption fire BEFORE the free-params bailout.
    ptFree = false
    for pt0 in paramTypes {
      if builder.typechecker.hasFreeVars(pt0) {
        ptFree = true
      }
    }
    // An ADAPTABLE variables-derived name (AnyInt pieces — the def
    // walk's unsuffixed-literal bindings) is never what a call site
    // composes: call wants are gated ground+non-adaptable, so the
    // derived name would emit a spec no call can reach (gf2ModVect's
    // (Uint,u32,u32) vs the demanded (u32,u32,u32)).
    ptAdapt = builder.typechecker.containsAdaptableType(paramTuple)
    if giWantName != "" && giWantName != specializedName &&
        (builder.alreadyEmitted(specializedName) || ptFree || ptAdapt) {
      // Only a COLLIDING variables-derived name may adopt the call
      // site's want: a unique name (e.g. manySignatures' value-named
      // specs) is correct as-is, and adopting would emit a duplicate
      // body under a foreign name.
      if !builder.alreadyEmitted(giWantName) && !isnull(giWantParams) {
        giWP = giWantParams!
        giList = arrayof(types.Type)
        if giWP.type == types.Type.TypeClass.Tuple {
          for giEl in giWP.tuple.elements {
            giList.append(giEl)
          }
        } else {
          giList.append(giWP)
        }
        giFree = false
        for giP in giList {
          if builder.typechecker.hasFreeVars(giP) {
            giFree = true
          }
        }
        giShapeOk = giList.length() == paramTypes.length()
        if giShapeOk {
          giI2 = 0u64
          for giP3 in paramTypes {
            // The want must be THIS function's shape — CLASS NAMES
            // only: a pending want from another call adopting through
            // the free-params path emitted Element's operator with
            // Foo's types (heapsort), but slotwise comparison would
            // reject the very signature splits the adoption exists
            // for (Integer(u64,..) want vs frozen Integer(u32,..)).
            giW3 = builder.typechecker.resolve(giList[giI2])
            giR3 = builder.typechecker.resolve(giP3)
            if (giW3.type == types.Type.TypeClass.Typename &&
                giR3.type == types.Type.TypeClass.Typename) {
              if giW3.tyname.name.name != giR3.tyname.name.name {
                giShapeOk = false
              }
            }
            // Non-class shapes (tuples, vars, ints) are not
            // class-identified — the name+arity checks suffice.
            giI2 += 1u64
          }
        }
        if !giFree && giShapeOk {
          giTuple = types.Type(types.TupleType(giList))
          giName = giTuple.genSpecializationName(self.sym.name)
          if giName == giWantName {
            specializedName = giName
            paramTypes = giList
            ptFree = false
            for giP2 in paramTypes {
              if giP2.type == types.Type.TypeClass.Tuple {
                builder.ensureTupleDeclared(self.location, giP2)
              }
            }
          }
        }
      }
    }
    // A parameter that does not resolve under these bindings (a variable
    // outside the poly's generalized set) cannot emit; the call site
    // emits this specialization on demand under its outer bindings.
    if ptFree {
      builder.deInstantiate(fntype.poly!)
      return
    }
    if builder.alreadyEmitted(specializedName) {
      builder.deInstantiate(fntype.poly!)
      return
    }
    // A parameter that resolved to a POLYMORPHIC type (a generic
    // function passed as a VALUE — funcptr's arraySum distancefunc)
    // has no C type of its own; this spec's ROUND binds the body's
    // application and resolves it to the concrete fn type.  Run the
    // round EARLY and re-read the slots (the later materializeFor
    // cache-hits).  Still-poly after the round => skip the spec: a
    // call site emits it once bindings resolve, and a null C type
    // panics cParameter.
    ppPoly = false
    for ppT in paramTypes {
      if builder.typechecker.resolve(ppT).type == types.Type.TypeClass.Polymorphic {
        ppPoly = true
      }
    }
    if ppPoly {
      builder.materializeFor(self, specializedName)
      ppNew = arrayof(types.Type)
      ppI = 0u64
      for ppVar in self.variables() {
        if isnull(ppVar.typedValue) {
          ppNew.append(paramTypes[ppI])
        } else {
          ppNew.append(builder.typechecker.resolve(ppVar.typedValue!))
        }
        ppI += 1u64
      }
      paramTypes = ppNew
      ppStill = false
      for ppT2 in paramTypes {
        if builder.typechecker.resolve(ppT2).type == types.Type.TypeClass.Polymorphic {
          ppStill = true
        }
      }
      if ppStill && !isnull(giWantParams) {
        // The call site RECOVERED the concrete fn type for this slot
        // (the recorded instantiation captured the callee's whole
        // poly); adopt it positionally.
        wpEls = arrayof(types.Type)
        giWP2 = giWantParams!
        if giWP2.type == types.Type.TypeClass.Tuple {
          for wpE in giWP2.tuple.elements {
            wpEls.append(wpE)
          }
        } else {
          wpEls.append(giWP2)
        }
        if wpEls.length() == paramTypes.length() {
          wpNew = arrayof(types.Type)
          wpI = 0u64
          for wpT in paramTypes {
            wpR = builder.typechecker.resolve(wpT)
            wpW = builder.typechecker.resolve(wpEls[wpI])
            if (wpR.type == types.Type.TypeClass.Polymorphic &&
                wpW.type == types.Type.TypeClass.Function &&
                !builder.typechecker.hasFreeVars(wpW)) {
              wpNew.append(wpW)
            } else {
              wpNew.append(wpT)
            }
            wpI += 1u64
          }
          paramTypes = wpNew
          ppStill = false
          for ppT3 in paramTypes {
            if builder.typechecker.resolve(ppT3).type == types.Type.TypeClass.Polymorphic {
              ppStill = true
            }
          }
        }
      }
      if ppStill {
        builder.deInstantiate(fntype.poly!)
        return
      }
    }
    builder.markEmitted(specializedName)
    builder.openScope()
    builder.openParamList()
    index = 0
    for variable in self.variables() {
      param = variable.genCParameter(builder, paramTypes[index])
      builder.addParam(param)
      index += 1
    }
    paramList = builder.closeParamList()
    // The function block may generate dependencies, e.g., inner
    // functions or tuples or structs. Ensure that these are listed
    // as depedencies of this function.
    builder.openDependencyList();
    savedTryBase = builder.openFunctionTryContext()
    prevEmittingFn = builder.currentEmittingFn
    prevEmittingName = builder.currentEmittingName
    prevEmittingResultWide = builder.currentEmittingResultWide
    polyPrevUsesF64x4 = self.usesF64x4
    polyPrevUsesU8x16 = self.usesU8x16
    self.usesF64x4 = false
    self.usesU8x16 = false
    builder.currentEmittingFn = self
    builder.currentEmittingName = specializedName
    prevEmittingParamTuple = builder.currentEmittingParamTuple
    prevEmittingParamFn = builder.currentEmittingParamFn
    builder.currentEmittingParamTuple = types.Type(types.TupleType(paramTypes))
    builder.currentEmittingParamFn = self
    builder.currentEmittingResultWide = builder.wideWidth(fntype.poly.scope.function.result)
    prevEmittingResultIsNone = builder.currentEmittingResultIsNone
    // Derive from the SAME source as the emitted signature (the baked
    // instantiation type when present; the scope otherwise).
    piResTy = builder.typechecker.resolve(fntype.poly.scope.function.result!)
    if !isnull(instantiation.type) {
      piInstTy = builder.typechecker.resolve(instantiation.type!)
      if piInstTy.type == types.Type.TypeClass.Function {
        piResTy = builder.typechecker.resolve(piInstTy.function.result!)
      }
    }
    builder.currentEmittingResultIsNone = false
    if piResTy.type == types.Type.TypeClass.Typename {
      if piResTy.tyname.name.name == "none" {
        builder.currentEmittingResultIsNone = true
      }
    }

    // Retypecheck the deferred body under THIS specialization's bindings
    // (the pre-emission pass only covers the first recorded instantiation).
    builder.materializeFor(self, specializedName)
    specFailed = false
    for failedSpec in self.failedSpecs {
      if failedSpec == specializedName {
        // The round for this specialization errored (an unreachable
        // typeswitch arm's recording, e.g. the turing test's
        // decParens(())); emitting its body would index or dereference
        // types that never checked.  Emit an EMPTY body: the declaration
        // satisfies any (equally unreachable) call-site reference.
        specFailed = true
      }
    }
    if specFailed {
      builder.openBlock(self.location)
      body = builder.closeBlock()
    } else {
      body = self.subBlock.genC(builder)
    }
    polyUsesF64x4 = self.usesF64x4
    polyUsesU8x16 = self.usesU8x16
    self.usesF64x4 = polyPrevUsesF64x4
    self.usesU8x16 = polyPrevUsesU8x16
    if self.sym.name == "decParens" {
      dbgA = "null"
      if !isnull(instantiation.type) {
        dbgATy = builder.typechecker.resolve(instantiation.type!)
        if dbgATy.type == types.Type.TypeClass.Function {
          dbgA = builder.typechecker.resolve(dbgATy.function.result!).toString()
        }
      }
      dbgB = builder.typechecker.resolve(fntype.poly.scope.function.result!).toString()
    }
    // Capture the signature's result and (when needed) derive it from
    // the emitted return path NOW, inside this spec's frame with the
    // ground param tuple still published: the recorded instantiation's
    // result re-resolves under foreign frames after deInstantiate.
    frameRes = null(types.Type)
    derivedRes = null(types.Type)
    if !specFailed {
      if !isnull(instantiation.type) {
        frTy = builder.typechecker.resolve(instantiation.type!)
        if frTy.type == types.Type.TypeClass.Function {
          frR = builder.typechecker.resolve(frTy.function.result!)
          if !builder.typechecker.hasFreeVars(frR) {
            frameRes = frR
          }
        }
      }
      // Derive unconditionally: an in-frame result can be ground yet
      // carry another round's binding (mulParens' sweep emissions); the
      // disagree-check below arbitrates.
      derivedRes = self.deriveSpecResult(builder, self.subBlock!)
    }
    builder.currentEmittingFn = prevEmittingFn
    builder.currentEmittingName = prevEmittingName
    builder.currentEmittingResultWide = prevEmittingResultWide
    builder.currentEmittingResultIsNone = prevEmittingResultIsNone
    builder.currentEmittingParamTuple = prevEmittingParamTuple
    builder.currentEmittingParamFn = prevEmittingParamFn
    builder.closeFunctionTryContext(savedTryBase)
    builder.deInstantiate(fntype.poly!)
    builder.closeScope()
    // This emission's round re-pointed the definition's variable slots;
    // put the enclosing emission's round back before returning to it.
    builder.restoreEmittingRound()
    newsym = Sym.new(specializedName)
    newsym.typedValue = instantiation.type
    if !isnull(giWantResult) && !specFailed && giWantName == specializedName {
      // A synthesized instantiation's recorded result re-resolves under
      // outer frames later; the call site's recovered result is ground.
      newsym.typedValue = types.Type(types.FunctionType(
          types.Type(types.TupleType(paramTypes)), giWantResult!))
    }
    specResFinal = frameRes
    if !isnull(derivedRes) && !specFailed {
      // The emitted return path is the authoritative result: apply when
      // the in-frame result is free or disagrees.
      drApply = true
      if !isnull(frameRes) {
        if frameRes.toString() == derivedRes.toString() {
          drApply = false
        }
      }
      if drApply {
        newsym.typedValue = types.Type(types.FunctionType(
            types.Type(types.TupleType(paramTypes)), derivedRes!))
        specResFinal = derivedRes
      }
    }
    if !isnull(specResFinal) && !specFailed {
      builder.recordSpecResult(specializedName, specResFinal!)
    }
    if isnull(newsym.typedValue) {
      // A synthesized instantiation may carry no type; the resolved scope
      // serves for the return type.
      newsym.typedValue = builder.typechecker.resolve(fntype.poly.scope)
    }
    if specFailed {
      // A failed (unreachable) specialization keeps a free result; give
      // the empty-bodied declaration a well-formed none signature.
      newsym.typedValue = types.Type(types.FunctionType(paramTuple,
          types.NONETY()))
    }
    fundec = builder.cFunDef(self.location, newsym, paramList, body,
        polyUsesF64x4, polyUsesU8x16)
    // A specialized result tuple has the same prototype-order requirement as
    // a monomorphic one, even when no tuple literal survives in its body.
    polyFnType = builder.typechecker.resolve(newsym.typedValue!)
    if polyFnType.type == types.Type.TypeClass.Function {
      polyResultType = builder.typechecker.resolve(polyFnType.function.result!)
      if polyResultType.type == types.Type.TypeClass.Tuple {
        builder.ensureTupleDeclared(self.location, polyResultType)
        fundec.addDependencyName(
            builder.typeGenerator.genCTuple(polyResultType.tuple!).name)
      } else if polyResultType.type == types.Type.TypeClass.Array {
        polyResultArray = builder.ensureArrayDeclared(
            self.location, polyResultType.array.elementType)
        fundec.addDependencyName(polyResultArray.typeName)
      }
    }
    // Class-typed parameters need the class struct typedef emitted first.
    for pt in paramTypes {
      if builder.isClassType(pt) {
        fundec.addDependencyName(builder.classCName(pt) + "_class")
      }
      if pt.type == types.Type.TypeClass.Tuple {
        fundec.addDependencyName(builder.typeGenerator.genCTuple(pt.tuple!).name)
      }
      if pt.type == types.Type.TypeClass.Array {
        ptDef = builder.ensureArrayDeclared(self.location, pt.array.elementType!)
        fundec.addDependencyName(ptDef.typeName)
      }
      if pt.type == types.Type.TypeClass.Function {
        // A function-pointer parameter needs its fnptr<N> typedef emitted.
        builder.ensureFunctionPtrDeclared(self.location, pt)
        fnptrCType = builder.typeGenerator.genCFunction(pt.function!)
        if !isnull(fnptrCType) {
          fundec.addDependencyName(fnptrCType.cDefinedType.name)
        }
      }
    }
    fundec.addDependencies(builder.topDependencyList().names)
    builder.closeDependencyList()
    builder.addDeclaration(fundec)
  }

  // Generate C code for a class: a struct typedef per instantiation, a
  // constructor function that heap-allocates and initializes an instance,
  // and the class's methods.
  func genCConstructor(self: Function, builder: c.CBuilder) {
    fntype = self.typedValue
    if isnull(fntype) {
      fntype = self.sym.typedValue
    }
    assert !isnull(fntype)
    info = builder.typechecker.lookupClass(self.sym)
    assert !isnull(info)

    if fntype.type == types.Type.TypeClass.Polymorphic {
      pi = fntype.poly.instantiations
      if !isnull(pi) {
        for instantiation in pi {
          builder.resolveInstantiation(instantiation)
          // Skip instantiations that are not concrete (e.g. recorded while
          // typechecking another polymorphic body); the concrete ones cover
          // every class that is actually constructed.
          if builder.instantiationIsConcrete(instantiation) {
            builder.instantiate(fntype.poly!, instantiation)
            // The bindings may leave the instance type itself unresolved:
            // a nested class records instantiations whose bindings cover
            // its own template slots but not the enclosing class's vars
            // inside a class-typed parameter (Entry's dict slot).  Emitting
            // would name the struct with free variables; a call site that
            // needs the specialization synthesizes it under bindings that
            // do resolve it.
            selfConcrete = true
            if !isnull(info.selfType) {
              if builder.typechecker.hasFreeVars(info.selfType!) {
                selfConcrete = false
              }
            }
            ctorPinned = arrayof(types.Type)
            if !selfConcrete && !isnull(instantiation.type) {
              // A NESTED class's instantiation covers its own template
              // slots but not the enclosing class's vars inside a
              // class-typed parameter (Entry's dict slot) — yet the
              // round-baked instantiation RESULT is the fully concrete
              // instance type.  Pin the selfType's residual free vars to
              // it for the duration of this emission (a manual
              // instantiate for the enclosing context), and unbind after.
              ctorInstTy = builder.typechecker.resolve(instantiation.type!)
              if ctorInstTy.type == types.Type.TypeClass.Function {
                ctorInstRes = builder.typechecker.resolve(ctorInstTy.function.result!)
                if (ctorInstRes.type == types.Type.TypeClass.Typename &&
                    !builder.typechecker.hasFreeVars(ctorInstRes)) {
                  ctorPinned = builder.typechecker.collectFreeTyvars(
                      builder.typechecker.resolve(info.selfType!), ctorPinned)
                  ctorPinTe = builder.typechecker.unify(info.selfType!, ctorInstRes)
                  if isnull(ctorPinTe) {
                    if !builder.typechecker.hasFreeVars(info.selfType!) {
                      selfConcrete = true
                    }
                  }
                }
              }
            }
            if selfConcrete {
              // Materialize the constructor body under this
              // instantiation's bindings (bypassing the deferred gate) —
              // but ONLY for classes with several concrete signatures:
              // the round re-derives field types per signature (reset +
              // snapshot), while a single-signature class's body keeps
              // its template bakes (the round would clobber the emitted
              // self-access types).
              if builder.classConcreteSigCount(info!) >= 2u64 {
                builder.typechecker.materializeSpec(self,
                    builder.classCtorCName(builder.typechecker.resolve(info.selfType!)))
              }
              self.genCConstructorInstance(builder, info!)
            }
            builder.deInstantiate(fntype.poly!)
            for ctorPin in ctorPinned {
              builder.typechecker.unifier.unbindVar(ctorPin.tyvar!)
            }
          }
        }
      }
    } else {
      self.genCConstructorInstance(builder, info!)
    }

    // Emit the methods for each of their own instantiations.
    for childfunc in self.childFunctions() {
      if childfunc.type == FuncType.Plain {
        childfunc.genCMethod(builder)
      } else if childfunc.type == FuncType.Operator {
        // Operators are standalone functions, not methods.
        childfunc.genCPlainFunc(builder)
      }
    }
  }

  // Generate the struct typedef and constructor function for one
  // instantiation of a class.  Must be called with the class's type
  // variables instantiated in the typechecker.
  func genCConstructorInstance(self: Function, builder: c.CBuilder, info: types.ClassInfo) {
    if builder.compactRegionEmission && !self.regionCompactEligible {
      builder.typechecker.typeErrorOnce(
          "internal: compact region constructor was not validator-approved",
          self.location)
      return
    }
    if isnull(info.selfType) {
      return
    }
    instType = builder.typechecker.resolve(info.selfType!)
    // The struct (and methods, final, toString) is named without phantom
    // parameters and emitted once; the constructor keeps them and emits per
    // argument type (its body's typeswitch picks a different conversion).  For
    // a class with no phantom parameters these names are identical, so the
    // behavior is unchanged.
    className = builder.classCName(instType)
    ctorName = builder.classCtorCName(instType)
    if builder.alreadyEmitted(ctorName) {
      return
    }
    structAlready = builder.alreadyEmitted(className)
    builder.markEmitted(ctorName)
    if !structAlready {
      builder.markEmitted(className)
    }

    if !structAlready {
    // The struct fields, with this instantiation's concrete types.  Field
    // types that name other declarations (structs, arrays, other classes)
    // must be emitted before this class.
    fieldDeps = arrayof(string)
    fields = c.CParamList()
    // Prefer this specialization's field snapshot (captured while the
    // constructor materialized under these bindings): the canonical slots
    // hold at most ONE signature's in-place bake.
    instBake = null(types.FieldBake)
    instSpec = instType.toString()
    for cfb in info.fieldBakes() {
      if cfb.spec == instSpec {
        instBake = cfb
      }
    }
    for i in range(info.strct.fieldnames.length()) {
      fieldty = builder.typechecker.resolve(info.strct.fieldtypes[i])
      if !isnull(instBake) {
        if i < instBake.types.length() {
          bakedFieldTy = builder.typechecker.resolve(instBake.types[i])
          if bakedFieldTy.type != types.Type.TypeClass.Var {
            fieldty = bakedFieldTy
          }
        }
      }
      if fieldty.type == types.Type.TypeClass.Var {
        // A field whose baked type stayed an open variable (e.g. its
        // create-on-assign unification reverted with the deferral
        // rollback): recover the type from the constructor body's
        // self.<field> = <expr> assignment.
        for fstmt in self.subBlock.statements() {
          if !isnull(fstmt.expr) {
            if fstmt.expr.type == ExprType.Equals {
              flhs = fstmt.expr.firstChildExpr!
              if flhs.type == ExprType.Dot {
                fbase = flhs.firstChildExpr!
                fmem = fbase.nextExprChildExpr!
                if (fbase.type == ExprType.Ident &&
                    fmem.type == ExprType.Ident) {
                  if fbase.val.symVal.name == "self" &&
                      fmem.val.symVal.name == info.strct.fieldnames[i].name {
                    frhs = flhs.nextExprChildExpr!
                    if !isnull(frhs.typedValue) {
                      fr = builder.typechecker.resolve(frhs.typedValue!)
                      if fr.type != types.Type.TypeClass.Var {
                        fieldty = fr
                        // Later retypechecks (the generated destroy) read
                        // the field type from the class info; persist it.
                        info.strct.fieldtypes[i] = fr
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
      ctype = builder.genCType(fieldty)
      if ctype.type == c.CTypeExpr.Type.Defined {
        fieldDeps.append(ctype.toString())
      } else if ctype.type == c.CTypeExpr.Type.Array {
        fieldDeps.append(ctype.cArrayType.name)
      } else if ctype.type == c.CTypeExpr.Type.Pointer {
        fieldDeps.append(ctype.cPointerType.target.toString() + "_class")
      }
      fieldParam = c.CParameter(self.location, info.strct.fieldnames[i].name, ctype!)
      for gname in info.generatedFieldNames {
        if gname == info.strct.fieldnames[i].name {
          fieldParam.noPrint = true
        }
      }
      fields.append(fieldParam)
    }
    toStringMethod = ""
    for childfunc in self.childFunctions() {
      if childfunc.sym.name == "toString" {
        toStringMethod = className + "_toString"
      }
    }
    refWidth = 0u32
    if !isnull(self.template) {
      if self.template.explicitWidth {
        refWidth = self.template.refWidth
      }
    }
    classDec = builder.cClassDeclaration(self.location, className, fields,
        toStringMethod, refWidth, self.sym.name, info)
    for depName in fieldDeps {
      classDec.addDependencyName(depName)
    }
    builder.addDeclaration(classDec)
    }

    // The constructor function: allocates the instance, runs the body, and
    // returns the instance pointer.  Named with phantom parameters kept, so
    // each argument-type variant is its own C function.
    builder.openScope()
    builder.openParamList()
    for variable in self.variables() {
      if variable.sym.name != "self" {
        pty = builder.typechecker.resolve(variable.typedValue!)
        param = variable.genCParameter(builder, pty)
        builder.addParam(param)
      }
    }
    paramList = builder.closeParamList()
    builder.openDependencyList()
    builder.openBlock(self.location)

    ctorPrevEmittingFn = builder.currentEmittingFn
    ctorPrevEmittingName = builder.currentEmittingName
    ctorPrevUsesF64x4 = self.usesF64x4
    ctorPrevUsesU8x16 = self.usesU8x16
    self.usesF64x4 = false
    self.usesU8x16 = false
    builder.currentEmittingFn = self
    builder.currentEmittingName = ctorName

    selfSym = Sym.new("self")
    ptrType = builder.genCType(instType)
    selfRef = builder.declareLocal(selfSym, ptrType)
    // Allocate from the per-class OBJECT pool: self = <class>_alloc_obj(),
    // which reuses a freed instance's memory when its id slot is handed back
    // (so instances behave like legacy pool slots — `a == b` after
    // `a.destroy(); b = Class()`), sets self->rn_id from the id free-list, and
    // seeds refCount = 1 (the creation reference; that count is what show()
    // prints as `nextFree`).  Reused memory is zeroed before the field
    // initializers below run.
    allocCall = builder.cCall(self.location, className + "_alloc_obj", null(c.CExpr))
    assign = c.CExpr(c.CBinaryOperator(self.location, c.CBinaryOperator.Type.Assign,
                                       null(c.CTypeExpr), selfRef, allocCall))
    builder.addStatement(self.location, assign)

    for statement in self.subBlock.statements() {
      statement.genC(builder)
    }

    retRef = c.CExpr(c.CIdentifier(selfSym, null(c.CTypeExpr), false, false, false))
    builder.addStatement(self.location, builder.cReturnStatement(self.location, retRef))
    body = builder.closeBlock()
    ctorUsesF64x4 = self.usesF64x4
    ctorUsesU8x16 = self.usesU8x16
    self.usesF64x4 = ctorPrevUsesF64x4
    self.usesU8x16 = ctorPrevUsesU8x16
    builder.currentEmittingFn = ctorPrevEmittingFn
    builder.currentEmittingName = ctorPrevEmittingName
    builder.closeScope()

    fundec = builder.cFunDefWithReturnType(self.location, ctorName, paramList,
        body, ptrType!, ctorUsesF64x4, ctorUsesU8x16)
    fundec.addDependencyName(className + "_class")
    fundec.addDependencies(builder.topDependencyList().names)
    builder.closeDependencyList()
    builder.addDeclaration(fundec)

    if !structAlready && !builder.compactRegionEmission {
    // A final() destructor is called from generated cleanup code, never
    // from user calls, so emit it per instantiation like toString below.
    for childfunc in self.childFunctions() {
      if childfunc.type == FuncType.Final {
        if isnull(childfunc.typedValue) {
          // Never typechecked (no user call site): demand it now so the
          // body's expressions carry types into emission.
          builder.typechecker.demandMethod(info, childfunc)
        }
        fty = childfunc.typedValue
        if isnull(fty) {
          fty = childfunc.sym.typedValue
        }
        if !isnull(fty) {
          if fty.type == types.Type.TypeClass.Polymorphic {
            ffn = builder.typechecker.resolve(fty.poly.scope)
          } else {
            ffn = fty!
          }
          if !builder.typechecker.hasFreeVars(ffn) {
            childfunc.genCMethodInstance(builder, ffn)
          }
        }
      }
    }

    // The generated destroy() runs relation removal and the final()
    // destructor; cleanup code calls it without a user call site, so emit
    // it per instantiation like final above.
    for childfunc in self.childFunctions() {
      if childfunc.sym.name == "destroy" && childfunc.type == FuncType.Plain {
        if isnull(childfunc.typedValue) {
          builder.typechecker.demandMethod(info, childfunc)
        }
        dty = childfunc.typedValue
        if isnull(dty) {
          dty = childfunc.sym.typedValue
        }
        if !isnull(dty) {
          if dty.type == types.Type.TypeClass.Polymorphic {
            // A deferred destroy resolves only under its (synthesized)
            // instantiation bindings.
            dInstE = dty.poly.instantiations.firstTyvarInstantiation
            if !isnull(dInstE) {
              builder.instantiate(dty.poly!, dInstE!)
              if childfunc.deferredTypecheck && !childfunc.deferredRetypechecked {
                // The recorded instantiation only resolves under this
                // class instantiation's bindings (the pre-emission pass
                // saw open variables); retypecheck here like the
                // genCPolyInstantiation fallback.
                builder.typechecker.retypecheckDeferred(childfunc)
                childfunc.deferredRetypechecked = true
              }
              dInstFn = builder.typechecker.resolve(dty.poly.scope)
              if !builder.typechecker.hasFreeVars(dInstFn) {
                childfunc.genCMethodInstance(builder, dInstFn)
              }
              builder.deInstantiate(dty.poly!)
              dfn = null(types.Type)
            } else {
              if childfunc.deferredTypecheck && !childfunc.deferredRetypechecked {
                // No instantiation was ever recorded (method dispatch does
                // not open the poly); the class bindings active here are
                // enough for the body to retypecheck concretely.
                builder.typechecker.retypecheckDeferred(childfunc)
                childfunc.deferredRetypechecked = true
              }
              dfn = builder.typechecker.resolve(dty.poly.scope)
            }
          } else {
            dfn = dty!
          }
          if !isnull(dfn) {
            if !builder.typechecker.hasFreeVars(dfn!) {
              childfunc.genCMethodInstance(builder, dfn!)
            }
          }
        }
      }
    }

    // A user-defined toString must be emitted for this instantiation even if
    // user code never calls it: the class's tostring_<class> print helper
    // references it.  The class's type variables are bound here, so the
    // method's type resolves concretely.
    for childfunc in self.childFunctions() {
      if childfunc.sym.name == "toString" && childfunc.type == FuncType.Plain {
        if isnull(childfunc.typedValue) {
          builder.typechecker.demandMethod(info, childfunc)
        }
        mty = childfunc.typedValue
        if isnull(mty) {
          mty = childfunc.sym.typedValue
        }
        if !isnull(mty) {
          if mty.type == types.Type.TypeClass.Polymorphic {
            mfn = builder.typechecker.resolve(mty.poly.scope)
          } else {
            mfn = mty!
          }
          if !builder.typechecker.hasFreeVars(mfn) {
            childfunc.genCMethodInstance(builder, mfn)
          }
        }
      }
    }
    }
  }

  // Generate C code for a class method, once per recorded instantiation.
  func genCMethod(self: Function, builder: c.CBuilder) {
    fntype = self.typedValue
    if isnull(fntype) {
      // The method was never called or typechecked; nothing to emit.
      return
    }
    if fntype.type == types.Type.TypeClass.Polymorphic {
      pi = fntype.poly.instantiations
      // For a method of a class with several concrete signatures, only
      // ROUND-BAKED recordings drive the sweep: an unbaked one still
      // chains through template variables and would freeze under ambient
      // leftover bindings into a cross-signature Frankenstein.
      msMulti = false
      if !isnull(self.parentFunction) {
        if self.parentFunction.type == FuncType.Constructor {
          msInfo = builder.typechecker.lookupClass(self.parentFunction.sym)
          if !isnull(msInfo) {
            if builder.classConcreteSigCount(msInfo!) >= 2u64 {
              msMulti = true
            }
          }
        }
      }
      if !isnull(pi) {
        for instantiation in pi {
          msSkip = msMulti && !instantiation.baked
          if !msSkip {
          builder.resolveInstantiation(instantiation)
          if builder.instantiationParamsConcrete(instantiation) {
            builder.instantiate(fntype.poly!, instantiation)
            // Pin unconstrained class-instance parameters (a relation
            // _compare's children stay bare knots under the recorded
            // bindings) so the body emits with concrete operand types --
            // the same pinning emitMethodOnDemand applies.  Without it the
            // sweep emits the body first with unresolved types (a pointer
            // compare instead of the class's operator <) and the name is
            // then already marked emitted.
            builder.concretizeMethodParams(self)
            // A synthesized instantiation can carry no resolved type (its
            // result slot stayed an unbound variable); resolve the scope
            // under the active bindings instead, like genCPolyInstantiation.
            mInstTy = instantiation.type
            if isnull(mInstTy) {
              mInstTy = builder.typechecker.resolve(fntype.poly.scope)
            }
            if !isnull(mInstTy) {
              mRepaired = self.repairMethodResult(builder, mInstTy!)
              // Emitting with residual free variables would compose invalid
              // C names/types; a call site that needs the specialization
              // synthesizes it under bindings that do resolve it
              // (emitMethodOnDemand applies the same gate).
              if (mRepaired.type == types.Type.TypeClass.Function &&
                  !builder.typechecker.hasFreeVars(mRepaired)) {
                self.genCMethodInstance(builder, mRepaired)
              }
            }
            builder.deInstantiate(fntype.poly!)
          }
          }
        }
      }
    } else if fntype.type == types.Type.TypeClass.Function {
      self.genCMethodInstance(builder, fntype!)
    }
  }

  // If |mfn|'s only unresolved piece is its result (a method like pop
  // returning a field of a bare-knot local: the result variable never
  // generalizes and its binding chain pops before emission), recover the
  // result through the method's own return expression and return a
  // repaired copy; otherwise return |mfn| unchanged.
  func repairMethodResult(self: Function, builder: c.CBuilder,
      mfn: types.Type) -> types.Type {
    if mfn.type != types.Type.TypeClass.Function {
      return mfn
    }
    // A bare-knot result needs the same repair as a free one: a recording
    // baked while its class was still single-signature keeps the knot
    // (findEntry's template result IS `Entry?`), and the knot is
    // ambiguous once a second signature exists.
    mrfResKnot = builder.typechecker.hasBareGenericKnot(mfn.function.result!)
    if !builder.typechecker.hasFreeVars(mfn!) && !mrfResKnot {
      return mfn
    }
    if builder.typechecker.hasFreeVars(mfn.function.params!) {
      return mfn
    }
    if isnull(self.subBlock) {
      return mfn
    }
    retExpr = findReturnExpr(self.subBlock!)
    if isnull(retExpr) {
      return mfn
    }
    retTy = retExpr.resolvedArgType(builder, 0u32, self.subBlock)
    if argTypeUnderResolved(builder, retTy) {
      return mfn
    }
    return types.Type(types.FunctionType(
        builder.typechecker.resolve(mfn.function.params!), retTy))
  }

  // Generate one specialization of a class method.  fnInstType is the
  // method's resolved function type, including the self parameter.
  func genCMethodInstance(self: Function, builder: c.CBuilder, fnInstType0: types.Type) {
    if builder.compactRegionEmission && !self.regionCompactEligible {
      builder.typechecker.typeErrorOnce(
          "internal: compact region method was not validator-approved",
          self.location)
      return
    }
    if fnInstType0.type != types.Type.TypeClass.Function {
      return
    }
    // Repair a free or bare-knot RESULT here, the single choke point:
    // several callers (the on-demand emitters) pass resolve(scope)
    // directly, and a knot result survives every other filter.
    fnInstType = self.repairMethodResult(builder, fnInstType0)
    methodName = builder.methodDefCName(fnInstType, self.sym.name)
    if builder.alreadyEmitted(methodName) {
      return
    }
    builder.markEmitted(methodName)

    builder.openScope()
    builder.openParamList()
    params = fnInstType.function.params
    index = 0
    for variable in self.variables() {
      if params.type == types.Type.TypeClass.Tuple {
        pty = params.tuple.elements[index]
      } else {
        pty = params
      }
      param = variable.genCParameter(builder, builder.typechecker.resolve(pty))
      builder.addParam(param)
      index += 1
    }
    paramList = builder.closeParamList()
    builder.openDependencyList()
    savedTryBase = builder.openFunctionTryContext()
    methodPrevEmittingFn = builder.currentEmittingFn
    methodPrevEmittingName = builder.currentEmittingName
    methodPrevUsesF64x4 = self.usesF64x4
    methodPrevUsesU8x16 = self.usesU8x16
    self.usesF64x4 = false
    self.usesU8x16 = false
    builder.currentEmittingFn = self
    builder.currentEmittingName = methodName
    // Publish this method instance's GROUND param tuple, exactly as
    // genCPolyInstantiation does: the body's variable slots hold the
    // template's bare knots (a relation compareElement's children), and
    // the recovery repairs operands positionally from here.
    methodPrevEmittingParamTuple = builder.currentEmittingParamTuple
    methodPrevEmittingParamFn = builder.currentEmittingParamFn
    mptParams = builder.typechecker.resolve(fnInstType.function.params!)
    mptElems = arrayof(types.Type)
    if mptParams.type == types.Type.TypeClass.Tuple {
      for mptE in mptParams.tuple.elements {
        mptElems.append(adoptConcreteBaseTy(builder,
            builder.typechecker.resolve(mptE)))
      }
    } else {
      mptElems.append(adoptConcreteBaseTy(builder, mptParams))
    }
    builder.currentEmittingParamTuple = types.Type(types.TupleType(mptElems))
    builder.currentEmittingParamFn = self
    // A deferred method (e.g. a relation's remove/insert whose `child`/`key`
    // parameter is unconstrained) re-typechecks its body under this concrete
    // instantiation's bindings before emission, exactly as deferred plain
    // functions do (see genCPolyInstantiation).  Otherwise the body emits
    // with leaked type variables -- e.g. an ambiguous `child.hash` left as a
    // fresh variable resolves to u64 only under the concrete instantiation,
    // and without this re-typecheck the call stays hashValue_v<N>, an
    // undeclared specialization.  Keyed per specialization: methodName is
    // computed from the parameter types, so each signature re-derives the
    // shared body expression types under its own bindings.
    builder.materializeFor(self, methodName)
    if self.sym.name == "destroy" {
      // destroy() must be idempotent: cleanup paths (cascade deletion,
      // reference overwrite, program exit) can reach an object more than
      // once.  Emit: if (self) { if (self->rn_id) { <body> rn_id = 0; } }
      // rn_id doubles as the object's identity (read by <u32>self, e.g. in a
      // final() print), so it must stay intact for the body and is reset last.
      // Mutual-cascade recursion is broken structurally instead: a cascade
      // clears its container slot before recursing (see builtin relation
      // transformers), so a re-entrant destroy finds nothing left to recurse
      // into.
      destroySelfSym = Sym.new("self")
      builder.openBlock(self.location)
      builder.openBlock(self.location)
      builder.openBlock(self.location)
      // Prevent a RE-ENTRANT destroy: the cleanup body runs removeChild,
      // whose `unref child` decrements refCount and would destroy this
      // object again (recursively, before rn_id is cleared below) if it
      // reached 0.  Bump refCount out of reach for the duration — the
      // object is being destroyed anyway.
      bumpSelfRef = c.CExpr(c.CIdentifier(destroySelfSym, null(c.CTypeExpr),
          false, false, false))
      bumpAssign = c.CExpr(c.CBinaryOperator(self.location,
          c.CBinaryOperator.Type.Assign, null(c.CTypeExpr),
          c.CExpr(c.CDotExpr(self.location, bumpSelfRef, "refCount", true)),
          builder.cIntLiteral(Bigint(0x40000000u32))))
      builder.addStatement(self.location, bumpAssign)
      for destroyStatement in self.subBlock.statements() {
        destroyStatement.genC(builder)
      }
      // Return this object's id slot to the per-class free-list so it is
      // reused by a later allocation (the body ran first, so final()
      // already read rn_id).  methodName is `<classCName>_destroy`.
      freeClassName = methodName
      if freeClassName.length() > 8u64 {
        freeClassName = freeClassName[0u64:freeClassName.length() - 8u64]
        freeIdSelf = c.CExpr(c.CIdentifier(destroySelfSym, null(c.CTypeExpr),
            false, false, false))
        freeArgs = c.CExprList()
        freeArgs.appendCExpr(c.CExpr(c.CDotExpr(self.location, freeIdSelf,
            "rn_id", true)))
        builder.addStatement(self.location, builder.cCall(self.location,
            freeClassName + "_free_id", c.CExpr(freeArgs)))
      }
      zeroSelfRef = c.CExpr(c.CIdentifier(destroySelfSym, null(c.CTypeExpr),
          false, false, false))
      zeroVal = Bigint(0u32)
      zeroAssign = c.CExpr(c.CBinaryOperator(self.location,
          c.CBinaryOperator.Type.Assign, null(c.CTypeExpr),
          c.CExpr(c.CDotExpr(self.location, zeroSelfRef, "rn_id", true)),
          builder.cIntLiteral(zeroVal)))
      builder.addStatement(self.location, zeroAssign)
      destroyGuardBody = builder.closeBlock()
      idSelfRef = c.CExpr(c.CIdentifier(destroySelfSym, null(c.CTypeExpr),
          false, false, false))
      idRef = c.CExpr(c.CDotExpr(self.location, idSelfRef, "rn_id", true))
      builder.addStatement(self.location, c.CStatement(self.location,
          c.CStatementType.If, idRef, destroyGuardBody))
      destroyNullBody = builder.closeBlock()
      condSelfRef = c.CExpr(c.CIdentifier(destroySelfSym, null(c.CTypeExpr),
          false, false, false))
      builder.addStatement(self.location, c.CStatement(self.location,
          c.CStatementType.If, condSelfRef, destroyNullBody))
      body = builder.closeBlock()
    } else {
      body = self.subBlock.genC(builder)
    }
    methodUsesF64x4 = self.usesF64x4
    methodUsesU8x16 = self.usesU8x16
    self.usesF64x4 = methodPrevUsesF64x4
    self.usesU8x16 = methodPrevUsesU8x16
    builder.currentEmittingFn = methodPrevEmittingFn
    builder.currentEmittingName = methodPrevEmittingName
    builder.currentEmittingParamTuple = methodPrevEmittingParamTuple
    builder.currentEmittingParamFn = methodPrevEmittingParamFn
    builder.closeFunctionTryContext(savedTryBase)
    builder.closeScope()

    resTy = builder.typechecker.resolve(fnInstType.function.result)
    if resTy.type == types.Type.TypeClass.Typename && resTy.tyname.name.name == "none" {
      returnType = builder.cVoidType(self.location)
    } else {
      returnType = builder.genCType(resTy)
    }
    fundec = builder.cFunDefWithReturnType(self.location, methodName, paramList,
        body, returnType!, methodUsesF64x4, methodUsesU8x16)
    if resTy.type == types.Type.TypeClass.Tuple {
      builder.ensureTupleDeclared(self.location, resTy)
      fundec.addDependencyName(
          builder.typeGenerator.genCTuple(resTy.tuple!).name)
    } else if resTy.type == types.Type.TypeClass.Array {
      methodResultArray = builder.ensureArrayDeclared(
          self.location, resTy.array.elementType)
      fundec.addDependencyName(methodResultArray.typeName)
    }
    // The method's self parameter needs the class struct typedef.
    params2 = fnInstType.function.params
    if params2.type == types.Type.TypeClass.Tuple && params2.tuple.elements.length() > 0 {
      fundec.addDependencyName(builder.classCName(params2.tuple.elements[0]) + "_class")
    } else {
      fundec.addDependencyName(builder.classCName(params2) + "_class")
    }
    // Array- and tuple-typed parameters need their typedefs first too.
    if params2.type == types.Type.TypeClass.Tuple {
      for p2el in params2.tuple.elements {
        p2r = builder.typechecker.resolve(p2el)
        if p2r.type == types.Type.TypeClass.Array {
          p2def = builder.ensureArrayDeclared(self.location, p2r.array.elementType!)
          fundec.addDependencyName(p2def.typeName)
        } else if p2r.type == types.Type.TypeClass.Tuple {
          builder.ensureTupleDeclared(self.location, p2r)
          fundec.addDependencyName(builder.typeGenerator.genCTuple(p2r.tuple!).name)
        }
      }
    }
    fundec.addDependencies(builder.topDependencyList().names)
    builder.closeDependencyList()
    builder.addDeclaration(fundec)
  }

  func genCStruct(self: Function, builder: c.CBuilder) {
    fntype = self.typedValue
    if isnull(fntype) {
      fntype = self.sym.typedValue
    }
    assert !isnull(fntype)

    if fntype.type == types.Type.TypeClass.Polymorphic {
      /******** Polymorphic function: iterate over instantiations *********/
       pi = fntype.poly.instantiations
       if !isnull(pi) {
         for instantiation in pi {
           builder.resolveInstantiation(instantiation)
           if builder.instantiationIsConcrete(instantiation) {
             self.genCStructInstance(builder, instantiation)
           }
         }
      }
    } else {
      /******************* Monomorphic function *************************/
      // open new scope - new parameter bindings are added to the scope
      // automatically
      builder.openScope()
      builder.openParamList()
      for variable in self.variables() {
        param = variable.genCParameter(builder)  // param is added to new scope
        builder.addParam(param)
      }
      paramList = builder.closeParamList()
      builder.openDependencyList();
      body = self.subBlock.genC(builder)
      builder.closeScope()
      fundec = builder.cStruct(self.location, self.sym, paramList, body)
      fundec.addDependencies(builder.topDependencyList().names)
      builder.closeDependencyList()
      builder.addDeclaration(fundec)
    }
  }

  // Emit one specialization of a struct: typedef, constructor and tostring.
  // The specialization name comes from the resolved field types, matching
  // genCTypename and call-site naming.
  func genCStructInstance(self: Function, builder: c.CBuilder,
                          instantiation: types.TyvarInstantiation) {
    fntype = self.typedValue!
    builder.instantiate(fntype.poly!, instantiation)
    paramTypes = arrayof(types.Type)
    for variable in self.variables() {
      paramTypes.append(builder.typechecker.resolve(variable.typedValue!))
    }
    paramTuple = types.Type(types.TupleType(paramTypes))
    specializedName = paramTuple.genSpecializationName(self.sym.name)
    if builder.alreadyEmitted(specializedName) {
      builder.deInstantiate(fntype.poly!)
      return
    }
    builder.markEmitted(specializedName)
    builder.openScope()
    builder.openParamList()
    index = 0
    for variable in self.variables() {
      param = variable.genCParameter(builder, paramTypes[index])
      builder.addParam(param)
      index += 1
    }
    paramList = builder.closeParamList()
    builder.openDependencyList();
    body = self.subBlock.genC(builder)
    builder.deInstantiate(fntype.poly!)
    builder.closeScope()
    fundec = builder.cStruct(self.location, Sym.new(specializedName), paramList, body)
    fundec.addDependencies(builder.topDependencyList().names)
    builder.closeDependencyList()
    builder.addDeclaration(fundec)
  }

  // Dump the function to stdout for debugging purposes.
  func dump(self) {
    text = ""
    self.dumpToString(text)
    print text
  }

  // Make a copy of the function in |destBlock|.
  func copy(self, destFunc: Function) -> Function {
    newFunction = Function(destFunc, self.type, self.sym, self.linkage, self.location)
    for variable in self.variables() {
      variable.copy(newFunction)
    }
    // Nested function definitions (e.g. a helper inside a transformer's
    // method template) copy along with their parent.
    for childFunction in self.childFunctions() {
      childFunction.copy(newFunction)
    }
    newBlock = self.subBlock.copy()
    newFunction.insertSubBlock(newBlock)
    if !isnull(self.typeExpr) {
      newFunction.typeExpr = self.typeExpr.copy()
    }
    // TODO: Comment this back in when we port templates.
    // if self.type == FuncType.Constructor {
      // self.templ.copy(newFunction)
    // }
    return newFunction
  }

  // Prepend a call statement to |childFunction| at the end of |function|.
  // |childFunction| will be called with no parameters.
  func prependFunctionCall(self, childFunction: Function) -> Statement {
    statement = self.appendFunctionCall(childFunction)
    // Move the statement to the start of the block.
    block = self.subBlock!
    block.removeStatement(statement)
    block.insertStatement(statement)
    return statement
  }

  // Append a call statement to |childFunction| at the end of |function|.
  // |childFunction| will be called with no parameters.
  func appendFunctionCall(self, childFunction: Function) -> Statement {
    ident = childFunction.firstNameIdent
    pathExpr = ident.createPathExpr()
    block = self.subBlock!
    location = Location(block.location.filepath!, 0u32, 0u32, 0u32)
    emptyParamsExpr = Expr(ExprType.List, location)
    callExpr = Expr.newBinary(ExprType.Call, pathExpr, emptyParamsExpr, location)
    statement = Statement(block, StateType.Call, location)
    statement.insertExpr(callExpr)
    return statement
  }

  // Generate a unique name for an identifier in the function, based on |name|.
  // Just use |name| if there is no conflict, otherwise, add _n, where n is an
  // integer to make the name unique in the function.
  func createUniqueSym(self, sym: Sym) -> Sym {
    if isnull(self.findIdent(sym)) {
      return sym
    }
    counter = 1u32
    do {
      newSym = Sym.new("%s_%u" % (sym.name, counter))
      counter += 1
    } while !isnull(self.findIdent(newSym))
    return newSym
  }

  // Create a path expression to this function.
  func createPathExpr(self) -> Expr {
    identExpr = Expr.newIdent(self.sym, self.location)
    parent = self.parentFunction
    if isnull(parent) {
      return identExpr
    }
    prefixExpr = parent.createPathExpr()
    return Expr.newBinary(ExprType.Dot, prefixExpr, identExpr, self.location)
  }
}

// Modules loaded by import statements, registered under their alias (the
// last identifier of the import path, or the name after "as").  The
// typechecker uses this to resolve <alias>.<member> expressions; modules
// otherwise share one global namespace in the bootstrap.
moduleAliases = arrayof(Sym)
moduleAliasTargets = arrayof(Function)

func registerModuleAlias(alias: Sym, moduleFunc: Function) {
  for i in range(moduleAliases.length()) {
    if moduleAliases[i] == alias {
      return
    }
  }
  moduleAliases.append(alias)
  moduleAliasTargets.append(moduleFunc)
}

func lookupModuleAlias(alias: Sym) -> Function? {
  for i in range(moduleAliases.length()) {
    if moduleAliases[i] == alias {
      return moduleAliasTargets[i]
    }
  }
  return null(Function)
}

// Container functions holding prependcode/appendcode code, indexed by the
// statement's codeBlockIndex (object comparisons are unreliable as keys).
codeBlockContainers = arrayof(Function)

func registerCodeBlock(statement: Statement, container: Function) {
  statement.codeBlockIndex = <u32>codeBlockContainers.length()
  codeBlockContainers.append(container)
}

func lookupCodeBlock(statement: Statement) -> Function? {
  if statement.codeBlockIndex == 0xffffffffu32 {
    return null(Function)
  }
  return codeBlockContainers[<u64>statement.codeBlockIndex]
}

// Append a call statement to the module initialization function in the root function.
func insertModuleInitializationCall(moduleFunc: Function) {
  pathExpr = moduleFunc.createPathExpr()
  block = moduleFunc.subBlock!
  location = Location(block.location.filepath!, 0u32, 0u32, 0u32)
  emptyParamsExpr = Expr(ExprType.List, location)
  callExpression = Expr.newBinary(ExprType.Call, pathExpr, emptyParamsExpr, location)
  block = getMainFunc().subBlock!
  statement = Statement(block, StateType.Call, location)
  statement.insertExpr(callExpression)
  // Move the statement to after the last initialization call.
  lastInitializer = root.lastInitializerStatement
  block.removeStatement(statement)
  if !isnull(lastInitializer) {
    block.insertAfterStatement(lastInitializer, statement)
  } else {
    block.insertStatement(statement)
  }
  root.lastInitializerStatement = statement
}

// The root function of the function tree.
relation OneToOne Root:"Main" Function:"Main" cascade
relation DoublyLinked Function:"Parent" Function:"Child" cascade
relation OneToOne Filepath:"Module" Function:"Module"

// Create the main function.
func createMainFunc() {
  rootFilepath = Filepath("Root filepath", null(Filepath), true)
  rootLocation = Location(rootFilepath, 0u32, 0u32, 0u32)
  mainFunc = Function(null(Function), FuncType.Package,
        Sym.new("main"), Linkage.Package, rootLocation)
  typeExpr = Expr(ExprType.IntType, rootLocation)
  typeExpr.width = 32u32
  mainFunc.insertTypeExpr(typeExpr)
  getRoot().insertMainFunction(mainFunc)
  rootBlock = mainFunc.subBlock!
  rootFilepath.insertModuleFunction(mainFunc)
  nullExpr = null(Expr(ExprType.Add, rootLocation))
  u32TypeExpr = Expr(ExprType.UintType, rootLocation)
  stringTypeExpr = Expr(ExprType.StringType, rootLocation)
  u32TypeExpr.width = 32u32
  argcVar = Variable(mainFunc, true, true, Sym.new("argc"), nullExpr, u32TypeExpr, true, rootLocation)
  argvVar = Variable(mainFunc, true, true, Sym.new("argv"), nullExpr,
      stringTypeExpr, true, rootLocation)
  statement = Statement(mainFunc.subBlock!, StateType.Return, rootLocation)
  retVal = Expr.newConstant(Value(0i32), rootLocation)
  statement.insertExpr(retVal)
}

root = getRoot()
createMainFunc()

unittest {
  use statement

  mainFunc = getMainFunc()
  rootLocation = mainFunc.location
  rootFilepath = rootLocation.filepath
  argcVar = mainFunc.firstVariable!
  argvVar = argcVar.nextFunctionVariable!

  func createEmptyFunction(owningFunc: Function?, name: string) -> Function {
    return Function(owningFunc, FuncType.Plain, Sym.new(name), Linkage.Module, rootLocation)
  }

  func createPrintlnFunction(owningFunc: Function?, name: string, text: string) {
    function = createEmptyFunction(owningFunc, name);
    block = function.subBlock!
    Statement(block, StateType.Println, rootLocation)
    return function
  }
}

unittest dumpTest {
  mainFunc.dump()
}

unittest copyTest {
  destFunc = createEmptyFunction(null(Function), "destFunc")
  destFunc = mainFunc.copy(destFunc)
  destFunc.dump()
}

unittest prependAndAppendFunctionCallTest {
  destFunc = createEmptyFunction(null(Function), "destFunc")
  mainCopy = mainFunc.copy(destFunc)
  // Create an inner function.
  topFunc = createEmptyFunction(mainCopy, "topFunc")
  botFunc = createEmptyFunction(mainCopy, "botFunc")
  lastStatement = mainCopy.appendFunctionCall(botFunc)
  firstStatement = mainCopy.prependFunctionCall(topFunc)
  subBlock = mainCopy.subBlock!
  assert subBlock.firstStatement == firstStatement
  assert subBlock.lastStatement == lastStatement
  mainCopy.dump()
}

unittest insertModuleInitializationCallTest {
  // Create an inner function.
  function1 = createEmptyFunction(getRoot().mainFunction!, "module1")
  function2 = createEmptyFunction(getRoot().mainFunction!, "module2")
  insertModuleInitializationCall(function1)
  insertModuleInitializationCall(function2)
  getMainFunc().dump()
}

unittest createUniqueSymTest {
  baseSym = Sym.new("printTest")
  topFunc = createEmptyFunction(getMainFunc(), "topFunc")
  createPrintlnFunction(topFunc, baseSym.name, "func1")
  newSym = topFunc.createUniqueSym(baseSym)
  assert newSym.name == "printTest_1"
  createPrintlnFunction(topFunc, newSym.name, "func2")
  newSym = topFunc.createUniqueSym(baseSym)
  assert newSym.name == "printTest_2"
  createPrintlnFunction(topFunc, newSym.name, "func3")
  topFunc.dump()
}

unittest createConstructorTest {
  mainFunc = getMainFunc()
  rootLocation = mainFunc.location
  rootFilepath = rootLocation.filepath
  constructor = Function(mainFunc, FuncType.Constructor, Sym.new("Foo"),
      Linkage.Module, rootLocation)
  template = Template(constructor, 16u32, rootLocation)
}

unittest newIteratorAndOperatorTest {
  itr = Function(mainFunc, FuncType.Iterator, Sym.new("testItr"), Linkage.Module, rootLocation)
  op = Function(mainFunc, FuncType.Operator, Sym.new("*"), Linkage.Module, rootLocation)
  op.opType = ExprType.Mul
  itr.dump()
  op.dump()
  itr.destroy()
  op.destroy()
}
