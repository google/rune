class ManagedRegionNode(self: ManagedRegionNode, allocationTag: u8) {
  func destroy(self) {
    println "cleanup"
  }
}

func allocateManagedNode(value: u64) -> u64 {
  node = ManagedRegionNode(0u8)
  return value
}

println withRegion(1u64, &allocateManagedNode(1u64), 0u64)
