func add(value: u64, context: u64) -> u64 {
  return value + context
}
items = [1u64, 2u64]
try {
  println parallelMap(items, 3u64, &add(items[0], 3u64), 2u64)
} except e {
  default => println "unexpected"
}
