class HasAdd(self, value: u64) {
  self.value = value
  operator + (left: HasAdd, right: HasAdd) -> HasAdd {
    return HasAdd(left.value + right.value)
  }
}
func bad(value: u64, context: u64) -> u64 {
  return value + context
}
items = [1u64]
parallelMap(items, 0u64, &bad(items[0], 0u64), 2u64)
